(function () {
    'use strict';

    angular.module('driver.map-layers.recent-events', [
        'Leaflet',
        'driver.config',
        'driver.state',
        'driver.map-layers',
        'driver.resources'
        ]);
})();
