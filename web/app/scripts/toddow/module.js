(function () {
    'use strict';

    /* ngInject */
    function DirectiveConfig() {
    }

    angular.module('driver.toddow', [
        'ui.router',
        'ui.bootstrap'
    ]).config(DirectiveConfig);

})();
