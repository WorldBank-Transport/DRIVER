(function () {
    'use strict';

    /* ngInject */
    function DirectiveConfig() {
    }

    angular.module('driver.recentProportions', [
        'driver.resources',
        'ui.bootstrap'
    ]).config(DirectiveConfig);

})();
