(function () {
    'use strict';

    /* ngInject */
    function RTListController() {
        var ctl = this;
        initialize();

        function initialize() {

        }
    }

    angular.module('ase.views.recordtype')
    .controller('RTListController', RTListController);
})();